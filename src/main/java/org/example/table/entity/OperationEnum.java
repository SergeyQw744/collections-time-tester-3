package org.example.table.entity;

/**
 * Перечисление OperationEnum
 * В это перечисление входят все методы, для которых происходит замер времени.
 */
public enum OperationEnum {
    ADD, GET, DELETE, SET, CONTAINS, SORT
}
